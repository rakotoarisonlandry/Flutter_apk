import 'package:flutter/material.dart';

class CategoriSection extends StatelessWidget {
  const CategoriSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 500,
      color: Colors.white,
    );
  }
}
